# 简化的部署工作流
# 适合快速上手，只包含基本的检查和构建

name: Simple Deploy

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: 📥 检出代码
              uses: actions/checkout@v4

            - name: 📦 设置 pnpm
              uses: pnpm/action-setup@v2
              with:
                  version: 8

            - name: 🟢 设置 Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "20"
                  cache: "pnpm"

            - name: 📚 安装依赖
              run: pnpm install --frozen-lockfile

            - name: 🔍 代码检查
              run: pnpm run lint

            - name: 🏗️ 构建项目
              run: pnpm run build

            - name: ✅ 构建成功
              run: echo "🎉 构建完成！可以开始配置部署了。"
